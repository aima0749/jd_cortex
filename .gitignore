*.pt
*.onnx
*.tar.bz2
*.pkl
*.wav
scene_state.json
posture_debug.log
__pycache__/
*.tmp
RE
# --- Model weights / binaries (large, auto-downloaded or generated locally) ---
*.pt
*.onnx
*.tar.bz2
*.pkl
*.wav
*.mp3
 
# --- Personal / biometric data (privacy - never commit) ---
setup/known_faces/
**/known_faces/
known_encodings.pkl
 
# --- Runtime-generated files (regenerated automatically, not source) ---
scene_state.json
posture_debug.log
*.tmp
**/tmp*.tmp
 
# --- Python caches / environments ---
__pycache__/
*.pyc
jd_env/
venv/
.venv/
 
# --- One-off exploration/testing scripts, not part of the real system ---
shared/diagnostics/
 
# --- Editor/OS junk ---
.vscode/
.idea/
Thumbs.db
.DS_Store
 # --- Witness diary (runtime data, personal observations - never commit) ---
memory/witness_diary.sqlite3*

# --- Cross-process speech queue (runtime, regenerated constantly) ---
speech_queue.json

# --- Live camera frames from the ARC plugin (runtime only) ---
bridge/

# runtime output, not source - changes every session
jd_robot_system/activity_log.txt